message("Kaggle Competition \n Optiver_Realized_Volatility_Prediction")
Sys.setenv(LANG = "eng")
options(
  prompt = "R> ",
  continue = " ",
  digits = 4,
  stringsAsFactors = F
)


# custom functions --------------------------------------------------------
.ftenv = new.env()

attach(.ftenv)
